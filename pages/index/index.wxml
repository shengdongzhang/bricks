<!--index.wxml-->
<scroll-view class="scroll" scroll-y>
  <view class="kind-list-item" wx:for="{{designInfo}}" wx:key="{{designInfo.id}}">
    <text>{{item.id}}</text>
    <image class="image-avatar" src="{{item.url}}" mode="cover"></image>
    <text>{{item.text}}</text>
  </view>
</scroll-view>
